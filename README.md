# MessengerDesktop
The desktop version of messenger

It is a port of the web version to windows platform using .NET Core 6.0.
Currently it is as functional as the web version but some UI issues needs to be fixed.
